import React from "react";
import PropTypes from "prop-types";

export const News = () => {
  return <div>ggwp no re</div>;
};

News.propTypes = {
  component: PropTypes.bool,
};

export default News;
