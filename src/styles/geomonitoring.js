import styled, { css } from "astroturf";

export const GeomonitoringContainer = styled("div")``;

const styles = css`
  .headerLeftSide {
    padding-right: 1rem;
    max-width: 34rem;
    p {
      margin-bottom: 0;
    }
  }
`;

export default styles;
